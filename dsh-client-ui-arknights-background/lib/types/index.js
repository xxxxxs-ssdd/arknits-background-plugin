/**
 * Arknights background plugin, node half.
 *
 * Deliberately empty: the plugin is browser-only. All effect lives in the
 * client bundle (src/client/index.ts), which injects a stylesheet painting the
 * wallpaper and gradient over the web shell; there is no host-side behavior to
 * register.
 */
/** Host plugin body — nothing to mount on the Node side. */
export function apply() { }
//# sourceMappingURL=index.js.map